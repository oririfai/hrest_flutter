[package]
name = "hrest_flutter"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
hrest-core = { path = "../../hrest-core", features = ["ffi"], version = "0.1.2" }
